{
  "name": "@app/inherit-files",
  "private": true,
  "scripts": {
    "start": "nodemon -x \"run-s build\"",
    "build": "run-s build:*",
    "build:clean": "rimraf dist",
    "build:tsc": "tsc -p ./tsconfig.json",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "26.0.13",
    "jest": "26.4.2",
    "nodemon": "2.0.4",
    "npm-run-all": "4.1.5",
    "rimraf": "3.0.2",
    "ts-jest": "26.3.0",
    "typescript": "4.0.2"
  }
}
